<!DOCTYPE html>
<html>
<head>
    <title>Giriş - İzin Takip Sistemi</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }
        .login-box {
            background: white;
            width: 300px;
            margin: 100px auto;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        h2 {
            text-align: center;
            color: #333;
        }
        input[type="text"], input[type="password"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 3px;
            box-sizing: border-box;
        }
        button {
            width: 100%;
            padding: 10px;
            background: #007bff;
            color: white;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }
        button:hover {
            background: #0056b3;
        }
        .error {
            color: red;
            text-align: center;
            margin: 10px 0;
        }
        .info {
            background: #e7f3ff;
            padding: 10px;
            border-radius: 3px;
            margin: 10px 0;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="login-box">
        <h2>İzin Takip Sistemi</h2>
        
        @if (ViewBag.Hata != null)
        {
            <div class="error">@ViewBag.Hata</div>
        }
        
        <form method="post">
            <input type="text" name="kullaniciAdi" placeholder="Kullanıcı Adı" required>
            <input type="password" name="sifre" placeholder="Şifre" required>
            <button type="submit">Giriş Yap</button>
        </form>
        
        <div class="info">
            <strong>Test Kullanıcıları:</strong><br>
            Admin: kullaniciAdi="admin", şifre="1234"<br>
            Kullanıcı: kullaniciAdi="user", şifre="1234"
        </div>
    </div>
</body>
</html> 